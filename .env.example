# copy to .env and change all credentials before sharing
POSTGRES_PASSWORD=local-example-change-me
SERVICE_TOKEN=local-service-token-change-me
KEYCLOAK_ADMIN_PASSWORD=local-admin-change-me
USER_PASSWORD=local-user-change-me
ADMIN_PASSWORD=local-admin-user-change-me
KEYCLOAK_PORT=18081
AGENT_PORT=18082
DASHBOARD_PORT=18083
LITELLM_MASTER_KEY=sk-local-example-change-me
LLM_MODEL=openai/configured-model
LLM_API_BASE=https://api.openai.com/v1
LLM_API_KEY=
MCP_NETWORK_PREFIX=10.246
# 실기기(솔루션 기기) 배치 전용. 기본(loopback)·CI에서는 비워둔다.
# 채우면 prepare.py가 자동으로 field 모드로 렌더링한다. README '실제 기기로 배치하기' 참고.
APPLIANCE_HOST=
APPLIANCE_BIND=
